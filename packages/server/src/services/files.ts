import { FileListItem } from "shared";

const getFilesList = (): FileListItem[] => {
  return [];
};

const deleteFile = (id: string) => {};

const downloadFile = (id: string) => {};

const uploadFile = () => {};

export default { getFilesList, deleteFile, downloadFile, uploadFile };
